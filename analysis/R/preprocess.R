# This is a placeholder for the preprocess function
# TODO: Write a proper preprocess function

preprocess <- function(df) {
  if (nrow(df) == 0) return(df)
  # add minimal checks / standard column names later
  df
}
