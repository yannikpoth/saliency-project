rl_fit_hierarchical <- function(data, stan_file, chains = 2, iter_warmup = 100, iter_sampling = 100, seed = 42) {
  stop("Model not implemented in stub.")
}
rl_summarize <- function(fit) tibble::tibble()
